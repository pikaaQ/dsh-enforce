**/node_modules

# ── dev/：开发与验证用的一切（工具脚本、调研产物、证据日志、隔离实例、备份）──────
# master 只保留"完整可用的插件"；这些不进版本库。
dev/

# ── OpenCode / 本地工具产物 ──────────────────────────────────
.omo/
.codegraph
patch/
.DS_Store
*.log
